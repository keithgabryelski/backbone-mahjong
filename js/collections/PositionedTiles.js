app.models.PositionedTiles = Backbone.Collection.extend({
  model: app.models.PositionedTile,
  position_tile: function(tile, position) {
  },
  remove_positioned_tiles: function(tile1, tile2) {
  }
});
